import { StyleSheet } from "react-native";

export const HeaderStyle = StyleSheet.create({
  container: {
    flexDirection: "row",
    paddingTop: 50,
    padding: 20,
  },
  backBtn: {
    justifyContent: "center",
  },
  headerTxt: {
    flex: 1,
    alignItems: "center",
  },
  subtitle: {
    fontSize: 20,
  },
});
